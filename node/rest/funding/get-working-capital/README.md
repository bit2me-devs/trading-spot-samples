# Get working capital limits

Retrieve configured working capital limits and daily usage for the authenticated user.

## Run

1. Install dependencies with `npm install`
2. Run the sample with `npm run start`
